<Window x:Class="DnDBattle.Views.HostGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🌐 Host Game (DM)" Height="700" Width="850"
        Background="#1A1A1D" FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner"
        Closing="Window_Closing">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Server Controls -->
        <Border Grid.Column="0" Grid.Row="0" Background="#252526" CornerRadius="8" Padding="12" Margin="0,0,6,8">
            <StackPanel>
                <TextBlock Text="Server Controls" FontSize="14" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Port:" Foreground="#CCC" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="PortInput" Width="80" Text="7777"
                             Background="#333" Foreground="White" BorderBrush="#555" Padding="4"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <Button x:Name="StartStopButton" Content="Start Server" Click="StartStopButton_Click"
                            Background="#0E639C" Foreground="White" Padding="12,6" Margin="0,0,8,0"
                            BorderThickness="0" Cursor="Hand"/>
                    <TextBlock x:Name="StatusText" Text="Stopped" Foreground="#CCC" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Token Assignment -->
        <Border Grid.Column="1" Grid.Row="0" Background="#252526" CornerRadius="8" Padding="12" Margin="6,0,0,8">
            <StackPanel>
                <TextBlock Text="Token Assignment" FontSize="14" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <ComboBox x:Name="PlayerCombo" Width="120" Margin="0,0,6,0"
                              Background="#333" Foreground="White" BorderBrush="#555"/>
                    <ComboBox x:Name="TokenCombo" Width="120" DisplayMemberPath="Name" Margin="0,0,6,0"
                              Background="#333" Foreground="White" BorderBrush="#555"/>
                    <Button Content="Assign" Click="AssignToken_Click"
                            Background="#0E639C" Foreground="White" Padding="8,4" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Connected Players -->
        <Border Grid.Column="0" Grid.Row="1" Background="#252526" CornerRadius="8" Padding="12" Margin="0,0,6,4">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Connected Players" FontSize="14" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                <ListView x:Name="PlayersListView" Background="#2D2D30" Foreground="White" BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Player" Width="120" DisplayMemberBinding="{Binding Username}"/>
                            <GridViewColumn Header="Tokens" Width="140" DisplayMemberBinding="{Binding AssignedTokenIds.Count}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </Border>

        <!-- Server Log + Chat -->
        <Border Grid.Column="1" Grid.Row="1" Background="#252526" CornerRadius="8" Padding="12" Margin="6,0,0,4">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Server Log" FontSize="14" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                <TextBox x:Name="ServerLogBox" IsReadOnly="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" AcceptsReturn="True"
                         Background="#2D2D30" Foreground="#CCC" BorderThickness="0"/>
            </DockPanel>
        </Border>

        <!-- Chat Section -->
        <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" Background="#252526" CornerRadius="8" Padding="12" Margin="0,4,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Chat" FontSize="14" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBox x:Name="ChatInput" Width="600"
                             Background="#333" Foreground="White" BorderBrush="#555" Padding="4"
                             KeyDown="ChatInput_KeyDown"/>
                    <Button Content="Send" Click="SendChat_Click" Margin="6,0,0,0"
                            Background="#0E639C" Foreground="White" Padding="12,4" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
                <ListBox x:Name="ChatHistory" Background="#2D2D30" Foreground="White" BorderThickness="0" Height="100"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
