<Window x:Class="DnDBattle.Views.Encounters.DatabaseImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Creatures to Database"
        Width="500" Height="400"
        WindowStartupLocation="CenterOwner"
        Background="#2D2D30">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Import JSON Creature Files" 
                   FontSize="18" FontWeight="Bold" Foreground="White"
                   Margin="0,0,0,15"/>

        <GroupBox Grid.Row="1" Header="Select JSON Folder" Foreground="White" Margin="0,0,0,15">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtFolderPath" Grid.Column="0" IsReadOnly="True"
                         Padding="5" VerticalContentAlignment="Center"/>
                <Button x:Name="BtnBrowse" Grid.Column="1" Content="Browse..."
                        Width="80" Margin="10,0,0,0" Click="BtnBrowse_Click"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Import Progress" Foreground="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="TxtLog" IsReadOnly="True"
                         Background="#1E1E1E" Foreground="#CCC"
                         TextWrapping="Wrap" BorderThickness="0"
                         VerticalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </GroupBox>

        <ProgressBar x:Name="ProgressBar" Grid.Row="3" Height="20" Margin="0,15,0,15"
                     Minimum="0" Maximum="100" Value="0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="TxtStatus" Foreground="#999" VerticalAlignment="Center"
                       Margin="0,0,15,0"/>
            <Button x:Name="BtnClear" Content="Clear Database" Width="110"
                    Margin="0,0,10,0" Click="BtnClear_Click"
                    ToolTip="Delete all creatures from the database"/>
            <Button x:Name="BtnImport" Content="Import All" Width="100" 
                    Margin="0,0,10,0" Click="BtnImport_Click" IsEnabled="False"/>
            <Button x:Name="BtnClose" Content="Close" Width="80" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
