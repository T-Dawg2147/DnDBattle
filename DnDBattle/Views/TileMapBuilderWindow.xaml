<Window x:Class="DnDBattle.Views.TileMapBuilderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:DnDBattle.Controls.TileMapBuilder"
        xmlns:vm="clr-namespace:DnDBattle.ViewModels"
        mc:Ignorable="d"
        Title="Tile Map Builder"
        Height="800" Width="1200"
        MinHeight="600" MinWidth="900"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner">

    <Window.DataContext>
        <vm:TileMapBuilderViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Tool Button Style -->
        <Style x:Key="ToolButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4FC3F7"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#4FC3F7"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5E5E62"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5E5E62"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4FC3F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewMapCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveMapCommand}"/>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding LoadMapCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#333337" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- File Operations -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="📄 New" 
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding NewMapCommand}"
                            ToolTip="New Map (Ctrl+N)"
                            Margin="0,0,4,0"/>
                    <Button Content="📂 Open" 
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding LoadMapCommand}"
                            ToolTip="Open Map (Ctrl+O)"
                            Margin="0,0,4,0"/>
                    <Button Content="💾 Save" 
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding SaveMapCommand}"
                            ToolTip="Save Map (Ctrl+S)"
                            Margin="0,0,4,0"/>

                    <Rectangle Width="1" Fill="#555" Margin="8,4"/>
                </StackPanel>

                <!-- Tools -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ToggleButton Content="🖌️"
                                  Style="{StaticResource ToolButtonStyle}"
                                  IsChecked="{Binding CurrentTool, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Paint}"
                                  ToolTip="Paint Tool (P)"
                                  Margin="0,0,4,0"/>
                    <ToggleButton Content="🧹"
                                  Style="{StaticResource ToolButtonStyle}"
                                  IsChecked="{Binding CurrentTool, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Erase}"
                                  ToolTip="Eraser Tool (E)"
                                  Margin="0,0,4,0"/>
                    <ToggleButton Content="💉"
                                  Style="{StaticResource ToolButtonStyle}"
                                  IsChecked="{Binding CurrentTool, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Pick}"
                                  ToolTip="Eyedropper/Pick Tool (I)"
                                  Margin="0,0,4,0"/>

                    <Rectangle Width="1" Fill="#555" Margin="8,4"/>

                    <Button Content="🔄"
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding RotateBrushCommand}"
                            ToolTip="Rotate Brush (R)"
                            Width="40"
                            Margin="0,0,4,0"/>

                    <TextBlock Text="{Binding BrushRotation, StringFormat={}{0}°}"
                               Foreground="#888"
                               VerticalAlignment="Center"
                               Margin="4,0,8,0"/>

                    <Rectangle Width="1" Fill="#555" Margin="8,4"/>
                </StackPanel>

                <!-- Map Name -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Map: " Foreground="#888"/>
                    <TextBox Text="{Binding CurrentMap.Name, UpdateSourceTrigger=PropertyChanged}"
                             Background="#3E3E42"
                             Foreground="White"
                             BorderBrush="#555"
                             Padding="8,4"
                             MinWidth="200"/>
                </StackPanel>

                <!-- Layer Selection -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="8,0">
                    <TextBlock Text="Layer:" Foreground="#888" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox SelectedIndex="{Binding CurrentLayer}"
                              Background="#3E3E42"
                              Foreground="White"
                              BorderBrush="#555"
                              Width="80">
                        <ComboBoxItem Content="0 - Base"/>
                        <ComboBoxItem Content="1 - Objects"/>
                        <ComboBoxItem Content="2 - Overlay"/>
                    </ComboBox>
                </StackPanel>

                <!-- Map Size -->
                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <TextBlock Text="Size:" Foreground="#888" VerticalAlignment="Center" Margin="8,0"/>
                    <TextBox Text="{Binding CurrentMap.WidthInSquares, UpdateSourceTrigger=PropertyChanged}"
                             Background="#3E3E42"
                             Foreground="White"
                             BorderBrush="#555"
                             Padding="4,4"
                             Width="40"
                             TextAlignment="Center"/>
                    <TextBlock Text="×" Foreground="#888" VerticalAlignment="Center" Margin="4,0"/>
                    <TextBox Text="{Binding CurrentMap.HeightInSquares, UpdateSourceTrigger=PropertyChanged}"
                             Background="#3E3E42"
                             Foreground="White"
                             BorderBrush="#555"
                             Padding="4,4"
                             Width="40"
                             TextAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="200" MaxWidth="400"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Tile Palette (Left Panel) -->
            <controls:TilePaletteControl Grid.Column="0" DataContext="{Binding}"/>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          Width="4" 
                          Background="#333337"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"/>

            <!-- Map Editor (Main Area) -->
            <controls:TileMapEditorControl Grid.Column="2" 
                                           x:Name="MapEditor"
                                           DataContext="{Binding}"/>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#333337" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusMessage}"
                           Foreground="#888"/>

                <TextBlock Grid.Column="1"
                           Foreground="#888"
                           Margin="16,0">
                    <Run Text="Tiles: "/>
                    <Run Text="{Binding CurrentMap.Tiles.Count, Mode=OneWay}"/>
                </TextBlock>

                <TextBlock Grid.Column="2"
                           Foreground="#666"
                           FontSize="10">
                    <Run Text="P=Paint E=Erase I=Pick R=Rotate"/>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>