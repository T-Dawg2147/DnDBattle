<Window x:Class="DnDBattle.Views.TagInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🏷️ Manage Tags" 
        Width="450" Height="380"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush_Background_Medium}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="TagToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{StaticResource Brush_Background_Hover}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush_Text_Primary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush_Border_Hover}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1B5E20"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush_Success}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#505054"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="🏷️ Manage Tags" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Brush_Text_Primary}"/>
            <TextBlock Text="Add tags to organize your content" Foreground="{StaticResource Brush_Text_Hint}" FontSize="11" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Add New Tag -->
        <Border Grid.Row="1" Background="{StaticResource Brush_Background_Control}" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtNewTag"
                         Grid.Column="0"
                         Padding="10,8" FontSize="13"
                         KeyDown="TxtNewTag_KeyDown"
                         ToolTip="Type a new tag and press Enter or click Add"/>

                <Button x:Name="BtnAddTag"
                        Grid.Column="1"
                        Content="➕ Add"
                        Padding="15,8" Margin="10,0,0,0"
                        Click="BtnAddTag_Click"
                        Style="{StaticResource SuccessButton}"/>
            </Grid>
        </Border>

        <!-- Tag Selection -->
        <Border Grid.Row="2" Background="{StaticResource Brush_Background_Control}" CornerRadius="6" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="QUICK TAGS" Foreground="{StaticResource Brush_Info}" FontWeight="Bold" FontSize="11" Margin="0,0,0,10"/>
                    <TextBlock Text="Click to toggle selection" Foreground="{StaticResource Brush_Text_Disabled}" FontSize="10" Margin="0,0,0,10"/>

                    <ItemsControl x:Name="TagButtonsPanel">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ToggleButton Content="{Binding Name}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Margin="0,0,8,8"
                                              Padding="12,6"
                                              MinWidth="60"
                                              Style="{StaticResource TagToggleButtonStyle}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="COMMON TAGS" Foreground="{StaticResource Brush_Info}" FontWeight="Bold" FontSize="11" Margin="0,15,0,10"/>

                    <WrapPanel x:Name="CommonTagsPanel" Orientation="Horizontal"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnCancel" Content="Cancel" 
                    Padding="20,10" Margin="0,0,10,0"
                    Click="BtnCancel_Click" IsCancel="True"/>
            <Button x:Name="BtnOk" Content="OK" 
                    Padding="20,10"
                    Click="BtnOk_Click" IsDefault="True"
                    Style="{StaticResource AccentButton}"
                    FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
