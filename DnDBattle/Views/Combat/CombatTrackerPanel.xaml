<UserControl x:Class="DnDBattle.Views.Combat.CombatTrackerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="280"
             Background="{StaticResource Brush_Background_Medium}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource Brush_Background_Dark}" Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="⚔️ Combat Tracker" 
                               FontSize="16" FontWeight="Bold" 
                               Foreground="{StaticResource Brush_Text_Primary}"/>
                    <TextBlock Text="{Binding CurrentRound, StringFormat='Round {0}', FallbackValue='Not in combat'}" 
                               FontSize="12" 
                               Foreground="{StaticResource Brush_Info}"
                               Visibility="{Binding IsInCombat, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>

                <!-- Start/End Combat Button -->
                <Button Grid.Column="1" 
                        Content="{Binding CombatButtonText}"
                        Command="{Binding ToggleCombatCommand}"
                        Padding="15,8"
                        FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource Brush_Success}"/>
                            <Setter Property="Foreground" Value="{StaticResource Brush_Text_Primary}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInCombat}" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Brush_Error}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Turn Controls (only visible during combat) -->
        <Border Grid.Row="1" Background="{StaticResource Brush_Background_Control}" Padding="10,8"
                Visibility="{Binding IsInCombat, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="◀ Prev" 
                        Command="{Binding PreviousTurnCommand}"
                        Padding="10,5" Margin="0,0,5,0"/>

                <Border Grid.Column="1" Background="{StaticResource Brush_Background_Medium}" CornerRadius="4" Padding="10,5">
                    <TextBlock Text="{Binding CurrentTurnToken.Name, FallbackValue='No creature'}" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource Brush_Info}"
                               HorizontalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </Border>

                <Button Grid.Column="2" Content="Next ▶" 
                        Command="{Binding NextTurnCommand}"
                        Padding="10,5" Margin="5,0,0,0"
                        FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource Brush_Success}"/>
                            <Setter Property="Foreground" Value="{StaticResource Brush_Text_Primary}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Initiative Order List -->
        <Border Grid.Row="2" Margin="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding InitiativeOrderList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,2" CornerRadius="4" Padding="10,8"
                                    MouseLeftButtonDown="InitiativeItem_Click"
                                    Cursor="Hand">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource Brush_Background_Control}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCurrentTurn}" Value="True">
                                                <Setter Property="Background" Value="{StaticResource Brush_Success}"/>
                                                <Setter Property="BorderBrush" Value="{StaticResource Brush_Success}"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource Brush_Background_Hover}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Initiative Number -->
                                    <Border Grid.Column="0" 
                                            Width="32" Height="32"
                                            CornerRadius="16"
                                            Background="{StaticResource Brush_Info}"
                                            Margin="0,0,8,0">
                                        <TextBlock Text="{Binding Initiative}" 
                                                   Foreground="{StaticResource Brush_Background_Darkest}"
                                                   FontWeight="Bold"
                                                   FontSize="14"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Name and Info -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" 
                                                   Foreground="{StaticResource Brush_Text_Primary}"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding HP, StringFormat='HP:  {0}'}" 
                                                       FontSize="11">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource Brush_Success}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HP}" Value="0">
                                                                <Setter Property="Foreground" Value="{StaticResource Brush_Error}"/>
                                                            </DataTrigger>
                                                            </Style.Triggers>
                                                    </Style>
                                                    </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Text="{Binding MaxHP, StringFormat='/{0}'}" 
                                                       FontSize="11" Foreground="{StaticResource Brush_Text_Hint}"/>
                                            <TextBlock Text="{Binding ArmorClass, StringFormat='  AC: {0}'}" 
                                                       FontSize="11" Foreground="{StaticResource Brush_Text_Hint}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Current Turn Indicator -->
                                    <TextBlock Grid.Column="2" 
                                               Text="▶" 
                                               FontSize="16"
                                               Foreground="{StaticResource Brush_Success}"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding IsCurrentTurn, Converter={StaticResource BoolToVis}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                        </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Footer with additional controls -->
        <Border Grid.Row="3" Background="{StaticResource Brush_Background_Dark}" Padding="10">
            <StackPanel>
                <Button Content="🎲 Reroll All Initiative" 
                        Command="{Binding RollAllInitiativeCommand}"
                        Padding="10,8"
                        Margin="0,0,0,5"
                        HorizontalAlignment="Stretch"/>

                <!-- Quick info -->
                <TextBlock HorizontalAlignment="Center" FontSize="11" Foreground="{StaticResource Brush_Text_Disabled}">
                    <Run Text="{Binding Tokens.Count, Mode=OneWay}"/>
                    <Run Text=" creatures in combat"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>