<UserControl x:Class="DnDBattle.Views.Combat.SelectedTokenPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DnDBattle.Views.Combat"
             mc:Ignorable="d" 
             d:DesignWidth="320"
             Background="{StaticResource Brush_Background_Medium}"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Stat Box Style -->
        <Style x:Key="StatBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Brush_Background_Control}"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Brush_Info}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="0,12,0,6"/>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Brush_Background_Hover}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush_Text_Primary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource Brush_Background_Hover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource Brush_Background_Hover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Quick Action Button Style (smaller) -->
        <Style x:Key="QuickActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- No Token Selected Message -->
        <TextBlock x:Name="NoSelectionMessage"
                   Text="Select a token on the map"
                   Foreground="{StaticResource Brush_Text_Disabled}"
                   FontSize="12"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Visible"/>

        <!-- Token Details Panel -->
        <ScrollViewer x:Name="TokenDetailsScroller"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="Collapsed">
            <StackPanel Margin="12,10">

                <!-- Targeting Mode Indicator -->
                <Border x:Name="TargetingIndicator" 
                        Background="{StaticResource Brush_Success}" 
                        CornerRadius="4" 
                        Padding="10,8" 
                        Margin="0,0,0,10"
                        Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock x:Name="TxtTargetingAction" 
                                       Foreground="{StaticResource Brush_Text_Primary}" 
                                       FontWeight="Bold" 
                                       FontSize="12"/>
                            <TextBlock Text="Click a target on the map" 
                                       Foreground="{StaticResource Brush_Success}" 
                                       FontSize="10"/>
                        </StackPanel>

                        <Button Grid.Column="1" 
                                Content="✕ Cancel" 
                                Padding="8,4"
                                Click="BtnCancelTargeting_Click"
                                Background="{StaticResource Brush_Error}"
                                Foreground="{StaticResource Brush_Text_Primary}"
                                BorderThickness="0"
                                Cursor="Hand"/>
                    </Grid>
                </Border>

                <!-- Header: Name and Type -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="TxtTokenName" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="{StaticResource Brush_Text_Primary}"
                                   TextWrapping="Wrap"/>
                        <TextBlock x:Name="TxtTokenSubtitle" 
                                   FontSize="10" 
                                   Foreground="{StaticResource Brush_Text_Hint}" 
                                   FontStyle="Italic"/>
                    </StackPanel>

                    <!-- Token Image -->
                    <Border Grid.Column="1" 
                            Width="40" Height="40" 
                            CornerRadius="4" 
                            Background="{StaticResource Brush_Background_Control}"
                            Margin="8,0,0,0">
                        <Image x:Name="ImgToken" 
                               Stretch="UniformToFill"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                </Grid>

                <!-- Conditions Bar -->
                <Border x:Name="ConditionsBar" 
                        Background="{StaticResource Brush_Background_Control}" 
                        CornerRadius="4" 
                        Padding="8,6" 
                        Margin="0,0,0,8"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Active Conditions" 
                                   FontSize="9" 
                                   Foreground="{StaticResource Brush_Text_Hint}" 
                                   Margin="0,0,0,4"/>
                        <WrapPanel x:Name="ConditionIconsPanel"/>
                    </StackPanel>
                </Border>

                <!-- Concentration Indicator -->
                <Border x:Name="ConcentrationBar" 
                        Background="{StaticResource Brush_Accent_Primary}" 
                        CornerRadius="4" 
                        Padding="8,6" 
                        Margin="0,0,0,8"
                        Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="🎯 Concentrating" 
                                       FontSize="10" 
                                       Foreground="{StaticResource Brush_Info}"/>
                            <TextBlock x:Name="TxtConcentrationSpell" 
                                       FontSize="12" 
                                       Foreground="{StaticResource Brush_Text_Primary}"
                                       FontWeight="SemiBold"/>
                        </StackPanel>

                        <Button Grid.Column="1" 
                                Content="✕" 
                                Width="24" Height="24"
                                ToolTip="Break Concentration"
                                Click="BtnBreakConcentration_Click"
                                Background="{StaticResource Brush_Error}"
                                Foreground="{StaticResource Brush_Text_Primary}"
                                BorderThickness="0"
                                FontWeight="Bold"/>
                    </Grid>
                </Border>

                <!-- Death Saves Panel -->
                <Border x:Name="DeathSavesPanel" 
                        Background="{StaticResource Brush_Background_Control}" 
                        CornerRadius="4" 
                        Padding="10,8" 
                        Margin="0,0,0,8"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="💀 Death Saving Throws" 
                                   FontSize="10" 
                                   Foreground="{StaticResource Brush_Text_Secondary}"
                                   Margin="0,0,0,8"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Successes -->
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Successes" Foreground="{StaticResource Brush_Success}" FontSize="9" HorizontalAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                                    <Ellipse x:Name="DeathSuccess1" Width="16" Height="16" Stroke="{StaticResource Brush_Success}" StrokeThickness="2" Margin="2"/>
                                    <Ellipse x:Name="DeathSuccess2" Width="16" Height="16" Stroke="{StaticResource Brush_Success}" StrokeThickness="2" Margin="2"/>
                                    <Ellipse x:Name="DeathSuccess3" Width="16" Height="16" Stroke="{StaticResource Brush_Success}" StrokeThickness="2" Margin="2"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Failures -->
                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                <TextBlock Text="Failures" Foreground="{StaticResource Brush_Error}" FontSize="9" HorizontalAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                                    <Ellipse x:Name="DeathFailure1" Width="16" Height="16" Stroke="{StaticResource Brush_Error}" StrokeThickness="2" Margin="2"/>
                                    <Ellipse x:Name="DeathFailure2" Width="16" Height="16" Stroke="{StaticResource Brush_Error}" StrokeThickness="2" Margin="2"/>
                                    <Ellipse x:Name="DeathFailure3" Width="16" Height="16" Stroke="{StaticResource Brush_Error}" StrokeThickness="2" Margin="2"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <!-- Status Text -->
                        <TextBlock x:Name="TxtDeathSaveStatus" 
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"
                                   FontWeight="Bold"/>

                        <!-- Death Save Button -->
                        <Button x:Name="BtnRollDeathSave"
                                Content="🎲 Roll Death Save" 
                                Margin="0,8,0,0"
                                Padding="10,6"
                                Click="BtnRollDeathSave_Click"
                                Background="{StaticResource Brush_Background_Hover}"
                                Foreground="{StaticResource Brush_Text_Primary}"
                                BorderThickness="0"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- HP Section -->
                <TextBlock Text="Hit Points" Style="{StaticResource SectionHeaderStyle}" Margin="0,4,0,6"/>
                <Border Background="{StaticResource Brush_Background_Dark}" CornerRadius="4" Padding="10,8">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="TxtCurrentHP" 
                                           FontSize="22" 
                                           FontWeight="Bold" 
                                           Foreground="{StaticResource Brush_Success}"/>
                                <TextBlock Text=" / " 
                                           FontSize="14" 
                                           Foreground="{StaticResource Brush_Text_Disabled}" 
                                           VerticalAlignment="Bottom" 
                                           Margin="0,0,0,3"/>
                                <TextBlock x:Name="TxtMaxHP" 
                                           FontSize="14" 
                                           Foreground="{StaticResource Brush_Text_Hint}" 
                                           VerticalAlignment="Bottom" 
                                           Margin="0,0,0,3"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="💚" 
                                        Width="28" Height="28" 
                                        Margin="0,0,4,0"
                                        ToolTip="Heal"
                                        Click="BtnHeal_Click"
                                        Style="{StaticResource ActionButtonStyle}"/>
                                <Button Content="💔" 
                                        Width="28" Height="28"
                                        ToolTip="Damage"
                                        Click="BtnDamage_Click"
                                        Style="{StaticResource ActionButtonStyle}"/>
                            </StackPanel>
                        </Grid>

                        <!-- HP Bar -->
                        <Grid Height="6" Margin="0,8,0,0">
                            <Border Background="{StaticResource Brush_Background_Medium}" CornerRadius="3"/>
                            <Border x:Name="HPBarFill" 
                                    Background="{StaticResource Brush_Success}" 
                                    CornerRadius="3" 
                                    HorizontalAlignment="Left"/>
                        </Grid>

                        <!-- Temp HP -->
                        <StackPanel x:Name="TempHPPanel" 
                                    Orientation="Horizontal" 
                                    Margin="0,6,0,0"
                                    Visibility="Collapsed">
                            <TextBlock Text="Temp HP: " Foreground="{StaticResource Brush_Text_Hint}" FontSize="10"/>
                            <TextBlock x:Name="TxtTempHP" Foreground="{StaticResource Brush_Info}" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Core Stats -->
                <TextBlock Text="Combat Stats" Style="{StaticResource SectionHeaderStyle}"/>
                <UniformGrid Columns="3">
                    <Border Style="{StaticResource StatBoxStyle}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="AC" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtAC" Foreground="{StaticResource Brush_Info}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatBoxStyle}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Initiative" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtInitiative" Foreground="{StaticResource Brush_Accent_Hover}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatBoxStyle}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="CR" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtCR" Foreground="{StaticResource Brush_Warning}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Ability Scores -->
                <TextBlock Text="Ability Scores" Style="{StaticResource SectionHeaderStyle}"/>
                <Border Background="{StaticResource Brush_Background_Dark}" CornerRadius="4" Padding="8">
                    <UniformGrid Columns="6">
                        <StackPanel HorizontalAlignment="Center" Margin="1">
                            <TextBlock Text="STR" Foreground="{StaticResource Brush_Text_Hint}" FontSize="8" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtStr" Foreground="{StaticResource Brush_Text_Primary}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtStrMod" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center" Margin="1">
                            <TextBlock Text="DEX" Foreground="{StaticResource Brush_Text_Hint}" FontSize="8" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtDex" Foreground="{StaticResource Brush_Text_Primary}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtDexMod" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center" Margin="1">
                            <TextBlock Text="CON" Foreground="{StaticResource Brush_Text_Hint}" FontSize="8" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtCon" Foreground="{StaticResource Brush_Text_Primary}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtConMod" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center" Margin="1">
                            <TextBlock Text="INT" Foreground="{StaticResource Brush_Text_Hint}" FontSize="8" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtInt" Foreground="{StaticResource Brush_Text_Primary}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtIntMod" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center" Margin="1">
                            <TextBlock Text="WIS" Foreground="{StaticResource Brush_Text_Hint}" FontSize="8" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtWis" Foreground="{StaticResource Brush_Text_Primary}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtWisMod" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center" Margin="1">
                            <TextBlock Text="CHA" Foreground="{StaticResource Brush_Text_Hint}" FontSize="8" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtCha" Foreground="{StaticResource Brush_Text_Primary}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtChaMod" Foreground="{StaticResource Brush_Text_Hint}" FontSize="9" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </UniformGrid>
                </Border>

                <!-- Speed -->
                <TextBlock Text="Speed" Style="{StaticResource SectionHeaderStyle}"/>
                <TextBlock x:Name="TxtSpeed" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="11" TextWrapping="Wrap"/>

                <!-- Movement (During Combat) -->
                <StackPanel x:Name="MovementSection" Visibility="Collapsed">
                    <TextBlock Text="Movement This Turn" Style="{StaticResource SectionHeaderStyle}"/>
                    <Border Background="{StaticResource Brush_Background_Dark}" CornerRadius="4" Padding="8">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="TxtMovementRemaining" Foreground="{StaticResource Brush_Info}" FontSize="11"/>
                                <Button Grid.Column="1" 
                                        Content="Reset" 
                                        FontSize="9" 
                                        Padding="6,2"
                                        Click="BtnResetMovement_Click"
                                        Style="{StaticResource QuickActionButtonStyle}"/>
                            </Grid>
                            <Grid Height="5" Margin="0,6,0,0">
                                <Border Background="{StaticResource Brush_Background_Medium}" CornerRadius="2"/>
                                <Border x:Name="MovementBarFill" 
                                        Background="{StaticResource Brush_Info}" 
                                        CornerRadius="2" 
                                        HorizontalAlignment="Left"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Legendary Actions Display -->
                <Border x:Name="LegendaryActionsPanel" 
                        Background="{StaticResource Brush_Background_Dark}" 
                        CornerRadius="4" 
                        Padding="10"
                        Margin="0,8,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="⭐ Legendary Actions" 
                                   Foreground="{StaticResource Brush_Warning}" 
                                   FontWeight="Bold" 
                                   FontSize="11"
                                   Margin="0,0,0,8"/>

                        <StackPanel x:Name="LegendaryPointsContainer" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Center" 
                                    Margin="0,0,0,8"/>

                        <TextBlock x:Name="TxtLegendaryRemaining" 
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource Brush_Text_Hint}"
                                   FontSize="10"/>

                        <Button x:Name="BtnResetLegendary"
                                Content="🔄 Reset"
                                Padding="8,4"
                                Margin="0,8,0,0"
                                Click="BtnResetLegendary_Click"
                                Background="{StaticResource Brush_Background_Hover}"
                                Foreground="{StaticResource Brush_Text_Primary}"
                                BorderThickness="0"
                                HorizontalAlignment="Center"
                                Cursor="Hand"
                                Visibility="Collapsed"/>
                    </StackPanel>
                </Border>

                <!-- Spell Slots Display -->
                <Border x:Name="SpellSlotsPanel" 
                        Background="{StaticResource Brush_Background_Dark}" 
                        CornerRadius="4" 
                        Padding="10"
                        Margin="0,8,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="✨ Spell Slots" 
                                   Foreground="{StaticResource Brush_Accent_Hover}" 
                                   FontWeight="Bold" 
                                   FontSize="11"
                                   Margin="0,0,0,8"/>

                        <WrapPanel x:Name="SpellSlotsContainer"/>

                        <Button Content="🌙 Long Rest"
                                Padding="8,4"
                                Margin="0,8,0,0"
                                Click="BtnLongRest_Click"
                                Background="{StaticResource Brush_Background_Hover}"
                                Foreground="{StaticResource Brush_Text_Primary}"
                                BorderThickness="0"
                                HorizontalAlignment="Left"
                                Cursor="Hand"/>
                    </StackPanel>
                </Border>

                <!-- Token Notes -->
                <Border x:Name="NotesPanel" 
                        Background="{StaticResource Brush_Background_Dark}" 
                        CornerRadius="4" 
                        Padding="10"
                        Margin="0,8,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="📝 Notes" Foreground="{StaticResource Brush_Info}" FontWeight="Bold" FontSize="11"/>

                            <Button Grid.Column="1" 
                                    Content="+" 
                                    Width="20" Height="20"
                                    Click="BtnAddNote_Click"
                                    Background="{StaticResource Brush_Success}"
                                    Foreground="{StaticResource Brush_Text_Primary}"
                                    BorderThickness="0"
                                    FontWeight="Bold"
                                    Cursor="Hand"/>
                        </Grid>

                        <StackPanel x:Name="NotesContainer"/>

                        <Grid x:Name="QuickAddNotePanel" Visibility="Collapsed" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="TxtQuickNote" 
                                     Background="{StaticResource Brush_Background_Medium}"
                                     Foreground="{StaticResource Brush_Text_Primary}"
                                     BorderBrush="{StaticResource Brush_Border_Normal}"
                                     Padding="8,6"
                                     KeyDown="TxtQuickNote_KeyDown"/>

                            <Button Grid.Column="1" 
                                    Content="✓" 
                                    Width="28"
                                    Margin="5,0,0,0"
                                    Click="BtnConfirmNote_Click"
                                    Background="{StaticResource Brush_Success}"
                                    Foreground="{StaticResource Brush_Text_Primary}"
                                    BorderThickness="0"
                                    FontWeight="Bold"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Actions Section -->
                <TextBlock Text="Actions" Style="{StaticResource SectionHeaderStyle}" Foreground="{StaticResource Brush_Error}"/>
                <StackPanel x:Name="ActionsPanel">
                    <TextBlock x:Name="NoActionsText" 
                               Text="No actions available" 
                               Foreground="{StaticResource Brush_Text_Disabled}" 
                               FontStyle="Italic"
                               FontSize="10"
                               Visibility="Collapsed"/>
                </StackPanel>

                <!-- Bonus Actions -->
                <StackPanel x:Name="BonusActionsSection" Visibility="Collapsed">
                    <TextBlock Text="Bonus Actions" Style="{StaticResource SectionHeaderStyle}" Foreground="{StaticResource Brush_Warning}"/>
                    <StackPanel x:Name="BonusActionsPanel"/>
                </StackPanel>

                <!-- Reactions -->
                <StackPanel x:Name="ReactionsSection" Visibility="Collapsed">
                    <TextBlock Text="Reactions" Style="{StaticResource SectionHeaderStyle}" Foreground="{StaticResource Brush_Accent_Primary}"/>
                    <StackPanel x:Name="ReactionsPanel"/>
                </StackPanel>

                <!-- Legendary Actions -->
                <StackPanel x:Name="LegendaryActionsSection" Visibility="Collapsed">
                    <TextBlock Text="Legendary Actions" Style="{StaticResource SectionHeaderStyle}" Foreground="{StaticResource Brush_Warning}"/>
                    <StackPanel x:Name="LegendaryActionsListPanel"/>
                </StackPanel>

                <!-- Quick Actions -->
                <TextBlock Text="Quick Actions" Style="{StaticResource SectionHeaderStyle}"/>
                <WrapPanel x:Name="QuickActionsPanel"/>

                <!-- Traits (Collapsible) -->
                <Expander x:Name="TraitsExpander" 
                          Header="Traits &amp; Features" 
                          Foreground="{StaticResource Brush_Info}"
                          FontSize="10"
                          Margin="0,8,0,0"
                          IsExpanded="False"
                          Visibility="Collapsed">
                    <Border Background="{StaticResource Brush_Background_Dark}" CornerRadius="4" Padding="8" Margin="0,4,0,0">
                        <TextBlock x:Name="TxtTraits" 
                                   Foreground="{StaticResource Brush_Text_Secondary}" 
                                   TextWrapping="Wrap" 
                                   FontSize="10"/>
                    </Border>
                </Expander>

                <!-- Notes (Collapsible) -->
                <Expander Header="DM Notes" 
                          Foreground="{StaticResource Brush_Info}"
                          FontSize="10"
                          Margin="0,8,0,0"
                          IsExpanded="False">
                    <Border Background="{StaticResource Brush_Background_Dark}" CornerRadius="4" Padding="8" Margin="0,4,0,0">
                        <TextBox x:Name="TxtDMNotes" 
                                 Background="Transparent"
                                 Foreground="{StaticResource Brush_Text_Secondary}"
                                 BorderThickness="0"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 FontSize="10"
                                 MinHeight="50"
                                 TextChanged="TxtNotes_TextChanged"/>
                    </Border>
                </Expander>

                <!-- Bottom Spacing -->
                <Border Height="15"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>