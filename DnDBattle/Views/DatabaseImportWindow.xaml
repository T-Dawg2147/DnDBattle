<Window x:Class="DnDBattle.Views.DatabaseImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📥 Import Creatures to Database"
        Width="550" Height="480"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush_Background_Medium}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="📥 Import Creature Files" 
                       FontSize="18" FontWeight="Bold" Foreground="{StaticResource Brush_Text_Primary}"/>
            <TextBlock Text="Import JSON creature files into your local database" 
                       Foreground="{StaticResource Brush_Text_Hint}" FontSize="11" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Folder Selection -->
        <Border Grid.Row="1" Background="{StaticResource Brush_Background_Control}" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📁 SELECT JSON FOLDER" Foreground="{StaticResource Brush_Info}" FontWeight="Bold" FontSize="11" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TxtFolderPath" Grid.Column="0" IsReadOnly="True"
                             Padding="10,8" VerticalContentAlignment="Center"/>
                    <Button x:Name="BtnBrowse" Grid.Column="1" Content="Browse..."
                            Padding="15,8" Margin="10,0,0,0" Click="BtnBrowse_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Progress Log -->
        <Border Grid.Row="2" Background="{StaticResource Brush_Background_Control}" CornerRadius="6" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="📋 IMPORT LOG" Foreground="{StaticResource Brush_Info}" FontWeight="Bold" FontSize="11" Margin="0,0,0,10"/>
                <Border Grid.Row="1" Background="{StaticResource Brush_Background_Medium}" CornerRadius="4">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBox x:Name="TxtLog" IsReadOnly="True"
                                 Background="Transparent" Foreground="{StaticResource Brush_Text_Secondary}"
                                 TextWrapping="Wrap" BorderThickness="0"
                                 FontFamily="Consolas" FontSize="11"
                                 Padding="10"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- Progress Bar -->
        <Border Grid.Row="3" Background="{StaticResource Brush_Background_Control}" CornerRadius="4" Padding="10" Margin="0,15,0,15">
            <StackPanel>
                <ProgressBar x:Name="ProgressBar" Height="8"
                             Minimum="0" Maximum="100" Value="0"/>
                <TextBlock x:Name="TxtStatus" Text="Select a folder to begin import" 
                           Foreground="{StaticResource Brush_Text_Hint}" FontSize="11" Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnClear" Content="🗑️ Clear Database" 
                    Padding="15,10" Margin="0,0,10,0" Click="BtnClear_Click"
                    Style="{StaticResource ErrorButton}"
                    ToolTip="Delete all creatures from the database"/>
            <Button x:Name="BtnImport" Content="📥 Import All" 
                    Padding="15,10" Margin="0,0,10,0" Click="BtnImport_Click" IsEnabled="False"
                    Style="{StaticResource SuccessButton}"
                    FontWeight="Bold"/>
            <Button x:Name="BtnClose" Content="Close" 
                    Padding="15,10" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
