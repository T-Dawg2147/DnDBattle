<UserControl x:Class="DnDBattle.Controls.TileMapBuilder.TileMapEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Focusable="True"
             Background="#1E1E1E">

    <Grid>
        <!-- Main Canvas Area -->
        <Border x:Name="CanvasBorder"
                Background="{Binding CurrentMap.BackgroundColor, FallbackValue=#1E1E1E}"
                ClipToBounds="True"
                MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
                MouseRightButtonDown="Canvas_MouseRightButtonDown"
                MouseMove="Canvas_MouseMove"
                MouseWheel="Canvas_MouseWheel">

            <Canvas x:Name="MapCanvas"
                    Background="Transparent"
                    ClipToBounds="False">

                <Canvas.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="ZoomTransform" ScaleX="1" ScaleY="1"/>
                        <TranslateTransform x:Name="PanTransform" X="0" Y="0"/>
                    </TransformGroup>
                </Canvas.RenderTransform>

                <!-- Background Grid Pattern -->
                <Rectangle x:Name="GridBackground"
                           Canvas.Left="0" Canvas.Top="0"
                           IsHitTestVisible="False"/>

                <!-- Tiles will be rendered here programmatically -->
                <Canvas x:Name="TilesCanvas" IsHitTestVisible="False"/>

                <!-- Grid Lines Overlay -->
                <Canvas x:Name="GridLinesCanvas" IsHitTestVisible="False"/>

                <!-- Preview/Ghost Tile -->
                <Border x:Name="PreviewTile"
                        Visibility="Collapsed"
                        IsHitTestVisible="False"
                        Opacity="0.6"
                        BorderBrush="#4FC3F7"
                        BorderThickness="2">
                    <Image x:Name="PreviewImage"
                           Stretch="Fill"
                           RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                </Border>

                <!-- Selection Rectangle (for future use) -->
                <Rectangle x:Name="SelectionRect"
                           Visibility="Collapsed"
                           Stroke="#4FC3F7"
                           StrokeThickness="1"
                           StrokeDashArray="4,2"
                           Fill="#204FC3F7"
                           IsHitTestVisible="False"/>
            </Canvas>
        </Border>

        <!-- Coordinates Display (bottom-left) -->
        <Border Background="#80000000"
                CornerRadius="4"
                Padding="8,4"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="10">
            <TextBlock x:Name="TxtCoordinates"
                       Text="X: 0, Y: 0"
                       Foreground="White"
                       FontFamily="Consolas"
                       FontSize="12"/>
        </Border>

        <!-- Zoom Display (bottom-right) -->
        <Border Background="#80000000"
                CornerRadius="4"
                Padding="8,4"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="-" 
                        Width="24" Height="24"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="ZoomOut_Click"/>
                <TextBlock x:Name="TxtZoom"
                           Text="100%"
                           Foreground="White"
                           FontFamily="Consolas"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="8,0"/>
                <Button Content="+"
                        Width="24" Height="24"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="ZoomIn_Click"/>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay"
                Background="#80000000"
                Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="Loading..." Foreground="White" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>