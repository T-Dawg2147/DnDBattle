<Window x:Class="DnDBattle.Views.Creatures.TagInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Tags" 
        Width="400" Height="300"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush_Background_Control}"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Add or select tags:"
                   Foreground="{StaticResource Brush_Text_Primary}" FontSize="14" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtNewTag"
                     Grid.Column="0"
                     Padding="8,6"
                     FontSize="13"
                     KeyDown="TxtNewTag_KeyDown"
                     ToolTip="Type a new tag and press Enter or click Add"/>

            <Button x:Name="BtnAddTag"
                    Grid.Column="1"
                    Content="Add"
                    Width="60"
                    Margin="8,0,0,0"
                    Click="BtnAddTag_Click"/>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource Brush_Background_Medium}" CornerRadius="4" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Quick Tags (click to toggle):"
                               Foreground="{StaticResource Brush_Text_Hint}" FontSize="11" Margin="0,0,0,8"/>

                    <ItemsControl x:Name="TagButtonsPanel">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ToggleButton Content="{Binding Name}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Margin="0,0,6,6"
                                              Padding="10,5"
                                              MinWidth="60"
                                              Style="{StaticResource TagToggleButtonStyle}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Common Tags:"
                               Foreground="{StaticResource Brush_Text_Hint}" FontSize="11" Margin="0,15,0,8"/>

                    <WrapPanel x:Name="CommonTagsPanel" Orientation="Horizontal">
                        
                    </WrapPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnOk" Content="OK" Width="80"
                    Margin="0,0,8,0" Click="BtnOk_Click" IsDefault="True"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="80"
                    Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>

    <Window.Resources>
        <Style x:Key="TagToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{StaticResource Brush_Background_Hover}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush_Text_Primary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush_Text_Disabled}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource Brush_Success}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush_Success}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource Brush_Background_Hover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
